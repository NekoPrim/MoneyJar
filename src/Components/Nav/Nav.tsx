import React from "react";

// this should prolly be used for the dropdown menu for navigating app
// there is a ticket for page header, but this should prolly be it
// make it a button with logo
// onClick opens dropdown (use mui)
export default function Nav() {
    return (
        <></>
    )
}