import React from 'react';
import logo from './logo.png';

export default function Header() {
    const standardMenu = [
        
    ];
    
    return (
        <div>
            <button>
                <img src={logo} />
            </button>
        </div>
    );
}